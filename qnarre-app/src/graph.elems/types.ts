import * as qt from '../../graph/types';

export {ColorBy, NdataT} from '../../graph/types';

export enum GraphIconT {
  CONST = 'CONST',
  LIST = 'LIST',
  META = 'META',
  OPER = 'OPER',
  SUMY = 'SUMY'
}
